.panel-body{:style => "display:#{(@website.platform == 'Javascript' || @platform_tab == 'javascript') ? 'block' : 'none'};"}
  %div
    %h4{:style => 'display:inline'} Note:
    %a If you’re using Node on the server, you’ll need
    %a{:href => "https://github.com/getsentry/raven-node"} raven-node.
  %hr
  %h5 Raven.js is distributed in a few different methods, and should get included after any other libraries are included, but before your own scripts.
  %pre
    %code.language-html
      %p &lt;script src="https://cdn.ravenjs.com/3.2.1/raven.min.js"&gt;&lt;/script&gt;
  %hr
  %h5 Configure the client:
  %pre
    %code.language-html
      =preserve do
        :escaped
          <script>
            Raven.config('https://#{@website.app_key}@#{app_domain}/#{@website.id}', {
                # we highly recommend restricting exceptions to a domain in order to filter out clutter
                whitelistUrls: [/example\.com/]
            }).install();
            </script>
  %h5 Add additional user context if it's available:
  %pre
    %code.language-html
      =preserve do
        :escaped
          Raven.setUser({
              email: 'user@example.com',
              id: '123'
          });
  %h5 Now call out to the epiclogger client to capture events:
  %pre
    %code.language-html
      =preserve do
        :escaped
          *record a simple message
          Raven.captureMessage('hello world!')

          *capture an exception
          try {
            errorThrowingCode();
          } catch(err) {
            Raven.captureException(err);
          }