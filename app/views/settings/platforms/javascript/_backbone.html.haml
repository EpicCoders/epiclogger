.panel-body{:style => "display:#{(@website.platform == 'Backbone' || @platform_tab == 'backbone') ? 'block' : 'none'};"}
  %div
    %h4{:style => 'display:inline'} Note:
    %a If you’re using Node on the server, you’ll need
    %a{:href => "https://github.com/getsentry/raven-node"} raven-node.
  %hr
  %h5 Start by adding the raven.js script tag to our page. You’ll want to position it after you load all other external libraries (like jQuery), but before your code.
  %pre
    %code.language-html
      =preserve do
        :escaped
          <script src="https://cdn.ravenjs.com/2.0.5/raven.min.js"></script>
  %br
  %h5 Configure the client:
  %pre
    %code.language-html
      =preserve do
        :escaped
          <script>
            Raven.config('https://#{@website.app_key}@#{app_domain}/#{@website.id}', {
                # we highly recommend restricting exceptions to a domain in order to filter out clutter
                whitelistUrls: [/example\.com/]
            }).install();
            </script>
  = render partial: 'settings/platforms/javascript/additional'