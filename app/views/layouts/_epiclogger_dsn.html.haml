:javascript
	Raven.config("#{ENV['EPICLOGGER_DSN']}", {
		whitelistUrls: [
			/epiclogger.com/
		],
	}).install();
- if current_user
	:javascript
		Raven.setUser({
			email: "#{current_user.email}",
			id: "#{current_user.id}"
		});